# 依赖与构建产物（容器内会重新安装与构建）
node_modules
.next
out

# 版本控制与本地环境
.git
.gitignore
.github
# 切勿将本地环境变量文件打入镜像（含真实 GITHUB_TOKEN 等机密，否则会随镜像层泄露到 ghcr.io）
.env*
.vscode
.idea

# 文档、截图、临时文件（非运行时所需）
# 注意：勿用 `*.md`，否则会误伤运行时需读取的 content/*.md（被 standalone 排除，必须显式 COPY 进镜像）
docs
old
*.log
npm-debug.log*
.DS_Store

# 本地证书（勿打入镜像）
certs

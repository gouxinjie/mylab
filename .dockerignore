# 依赖与构建产物（容器内会重新安装与构建）
node_modules
.next
out

# 版本控制与本地环境
.git
.gitignore
.github
.vscode
.idea

# 文档、截图、临时文件（非运行时所需）
# 注意：勿用 `*.md`，否则会误伤运行时需读取的 content/*.md（被 standalone 排除，必须显式 COPY 进镜像）
docs
old
*.log
npm-debug.log*
.DS_Store

# 本地证书（勿打入镜像）
certs
